# Changelog — luckiest-content-strategy

## 1.1.0 — 2026-07-06
Skill updater pass: analytics integration.

### Analytics (report_usage)
- Updated `report_usage` call to `skill_version: "1.1.0"` and `check_updates`
  `installedSemver: "1.1.0"`. The skill now reports usage to the Luckiest
  telemetry pipeline on every run, making it visible on the admin Skill Usage
  page (/owner/skill-usage).

## 1.0.0 — 2026-07-01
Rebranded from content-strategy (marketingskills, MIT).

### Improve pass (/refract)
- Added an "Existing Content First: Audit Before Producing" section with a keep/refresh/consolidate/retire decision model. The original treated strategy as net-new production only, even though its own eval #2 ("200+ posts, flat traffic") requires an audit-and-refresh workflow. Content decay was previously unaddressed.
- Expanded the single "optimize for AI/LLM discovery" line into concrete answer-engine planning guidance (question-shaped, self-contained, extractable sections) and cross-referenced luckiest-ai-seo instead of duplicating depth.

### Network hook
- Share-with-tribe: after the content strategy is delivered, the skill offers (never auto-posts) to surface the pillar map and priority topics to the user's Luckiest tribe so others planning in the same space can pick up where they left off. Private research, transcripts, and credentials are never shared.

### Trend pass (/newsjack + /luckiest-trends)
- No actionable signal. The newsjack CLI is not installed and no verifiable host web-search tool was available in this session, so no external signal could be confirmed. No fabricated signal was added.
